alpaca-py>=0.21
pydantic>=2.5
python-dotenv>=1.0
PyYAML>=6.0
yfinance>=0.2
pytest>=8.0
tzdata>=2024.1   # zoneinfo data for America/Chicago on Windows
# The vectorbt backtest stack is heavy (numba/llvmlite) and only needed for
# backtest/backtest_rules.py: pip install -r backtest/requirements.txt
