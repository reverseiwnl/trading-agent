# Daily digest — 2026-06-11

_Paper trading. Written by routine error handler — src/report.py could not run (see errors below)._

## Portfolio snapshot

**UNAVAILABLE** — Alpaca paper account unreachable (see errors below).

## System vs VOO counterfactual

**UNAVAILABLE** — no market data fetched today.

## Positions

**UNAVAILABLE** — Alpaca paper account unreachable.

## Today's signals

No signals file for today — the research step did not run (fetch_data.py failed before research).

## Decision engine verdicts

No decisions recorded — the decision engine did not run today.

## Execution results

No executions recorded today.

## Data & run errors

**This run failed completely.** Two independent root causes prevented all data fetch, research, and reporting:

### 1. Missing `.env` file — Alpaca credentials not configured

The `.env` file is absent from this cloud execution environment. Without `ALPACA_API_KEY` and `ALPACA_SECRET_KEY`, all Alpaca API calls (market data, account state, news) fail immediately. The `.env.example` documents the required variables:

- `ALPACA_API_KEY` — Alpaca paper trading API key
- `ALPACA_SECRET_KEY` — Alpaca paper trading secret key
- `ALPACA_BASE_URL=https://paper-api.alpaca.markets`
- `FINNHUB_API_KEY` — optional, for news

**Fix:** Add a `.env` file with real paper trading credentials to the cloud environment (via environment variables in the session/routine configuration — never commit the file).

### 2. Network policy — all external hosts blocked (HTTP 403: Host not in allowlist)

Every outbound HTTP request returned `HTTP Error 403: Host not in allowlist`. Affected:
- Alpaca market data API (`data.alpaca.markets`)
- Alpaca broker/account API (`paper-api.alpaca.markets`)
- Yahoo Finance (yfinance fallback)
- News APIs (Finnhub, Benzinga)

This is a cloud environment network policy issue — the environment was not configured to allow access to the required external hosts.

**Fix:** Add the following hosts to the network allowlist in the routine/environment configuration:
- `paper-api.alpaca.markets`
- `data.alpaca.markets`
- `query1.finance.yahoo.com` (yfinance)
- `finnhub.io` (optional news)

### fetch_data.py output (verbatim)

```
AAPL: failed (alpaca bars/price failed: Host not in allowlist; yfinance bars/price fallback failed: argument of type 'NoneType' is not iterable; fundamentals failed: argument of type 'NoneType' is not iterable; alpaca news failed: Host not in allowlist)
GOOGL: failed (same pattern)
JNJ: failed (same pattern)
MSFT: failed (same pattern)
NVDA: failed (same pattern)
UNH: failed (same pattern)
VOO: failed (same pattern)
ERROR: could not fetch holdings from Alpaca paper account: Host not in allowlist
wrote 7 snapshots to data/2026-06-11 — PARTIAL PULL (exit code 1)
```

### report.py output (verbatim)

```
FATAL: could not read paper account, no digest written: Host not in allowlist
(exit code 2)
```

## Action required

This routine cannot function until both issues above are resolved. No trades were considered, no signals were generated, no orders are pending. The paper account state is unchanged from 2026-06-10.
